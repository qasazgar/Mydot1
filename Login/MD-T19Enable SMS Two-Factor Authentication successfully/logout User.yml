info:
  name: logout User
  type: http
  seq: 4

http:
  method: post
  url: https://api-stage.dpsb.ir/mydot/api/v1/auth/logout/
  headers:
    - name: accept
      value: application/json
    - name: X-CSRFTOKEN
      value: eywcsm6peklSGpfxEX8c5Ga6GFdR8kYel0HSQ4k86aFmK8YApf79bZqpayl4L2zj
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        tests :{
          test("Status code should be 200", function() {
            expect(res.status).to.equal(200);

          });

          test("Response time should be less than 500 ms", function() {
            expect(res.responseTime).to.be.lessThan(500);
          });
        }

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
